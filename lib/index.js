// dsh-notify-sound — host 端插件（Node 进程内运行）。
// 提示音功能全部在浏览器端（lib/client.js）实现，host 端无需任何逻辑。
/** Required services: none. */
const inject = [];
/** Host plugin body: nothing to do. */
function apply(_ctx) {}
export { apply, inject };
